apiVersion: v1
kind: Service
metadata:
    name: devopscourseprojectnode
spec:
    type: NodePort
    selector:
        app: devopscourseproject-api
    ports:
        - protocol: TCP
          port: 8080
          targetPort: 8080
