apiVersion: apps/v1
kind: Deployment
metadata:
    name: devopscourseproject
spec:
    selector:
        matchLabels:
            app: DevOps_Course_Project
    replicas: 3
    template:
        metadata:
            labels:
                app: DevOps_Course_Project
        spec:
            containers:
                - name: devopscourseproject-api
                  image: ${{ secrets.DOCKERHUB_USERNAME }}/devopscourseproject
                  ports:
                      - containerPort: 5000
